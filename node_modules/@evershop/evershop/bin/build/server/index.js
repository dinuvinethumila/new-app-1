const webpack = require('webpack');
const {
  createConfigServer
} = require('@evershop/evershop/src/lib/webpack/prod/createConfigServer');
const { error } = require('@evershop/evershop/src/lib/log/logger');
/* add by dinuvi*/
app.get('/',(req , res) => {
  res.send('hello');
});

app.get('/api/message',(requires) => {
  res.json ({message: 'hellow from back'});
  });

module.exports.buildServer = async function buildServer(routes) {
  const config = createConfigServer(routes);
  const compiler = webpack(config);

  return new Promise((resolve, reject) => {
    compiler.run((err, stats) => {
      if (err || stats.hasErrors()) {
        error(
          stats.toString({
            errorDetails: true,
            warnings: true
          })
        );
        reject(err);
      }
      resolve(stats);
    });
  });
};
